# 下载器流量统计 发布说明

## v1.0.4（2026-08-21）

### 概述

本版本解决两点：①采集诊断日志脱敏；②修复主界面「上传/下载总览」与业务日志统计口径不一致的问题。均为后端改动，无需重建前端。

### 变更

**1. 日志脱敏**

- 采集时的「样本种子取值」日志不再打印完整 tracker 地址（其中含 passkey），改为只显示域名，避免敏感信息泄露进 MP 业务日志。

**2. 统计口径统一（修复总览与日志不一致）**

- 现象：业务日志显示「本月上传 2.15 GB」，而主界面「本月上传」显示 6.65 GB，两者不一致。
- 根因：主界面总览取自各站点行之和（`api_records.total_uploaded`），而业务日志与限速阈值判断以 GLOBAL 汇总行为准。当库里存在历史 bug 期写下的重复 / 别名站点行时，站点行之和会虚高于 GLOBAL。
- 修复：主界面「上传 / 下载总览」改为与采集入账、限速阈值判断**同一口径（GLOBAL 汇总行）**，确保主界面与业务日志数字一致；真实月总量以 GLOBAL 为准。

### 升级说明

- 版本号 **1.0.3 → 1.0.4**；纯后端改动，**无需重建前端 dist**。在 MP 更新到 1.0.4 并重启 / 重载插件即可。
- 若希望顺带把站点明细行也重新归一到正确站点名，可升完后在配置页用「清空历史数据」（带二次确认）清理一次，重新采集合计；如不清理也不影响统计口径（总览、阈值、日志均已以 GLOBAL 为准）。

---
## v1.0.3（2026-08-21）

### 概述

清理发布记录：历史上个别版本说明中出现了不便于公开的站点名与下载地址，本版本将相关说明收敛为模板化描述，后续版本记录统一不再出现任何站点名或地址链接。功能无改动。

---
## v1.0.2（2026-08-20）

### 概述

修复个别站点流量被错误归到其它站点的问题。问题定位与修复均聚焦于 tracker→站点名的归属匹配规则，纯后端改动，前端无需改。v1.0.1 的历史数据保留清理 / 二次确认清空功能不受影响。

### Bug 修复

**错误的站点归属（避免提及具体站点）**

- 现象：部分种子的流量被归到错误的站点。
- 根因：`_map_domain_to_site` 原用宽松的**子串 `in` 双向匹配**（`sd in host or host in sd`），不检查域名边界，导致较长 tracker 域名的子串可能命中其它站点。
- 修复：改为**主机名边界匹配**——tracker host 与站点域名**相等**，或**互为子域**（`host.endswith("." + sd)` / `sd.endswith("." + host)`，兼容 `www./tracker./on.` 等子域前缀差）；命中后在**多个候选取「域名字串最长」**的最具体一个，子串域名不再造成误归。
- 附带：新增**一次性站点映射诊断日志**，只对前几个不同的 tracker 域名各打印一次 `站点映射诊断 host=... 候选=[...] => 命中「...」`，便于核对归属结果（仅输出到本机 MP 业务日志）。

### 交付说明

- 版本号 **1.0.1 → 1.0.2**；`package.v2.json`、README、RELEASE_NOTES 已同步。
- 纯后端改动：**无需重建前端 dist**；在 MP 更新到 1.0.2 并重启 / 重载插件即可。
- 注意：本次修复只影响**之后**的采集归类；修复前已错误的当天历史行不会自动改名，可在确认后使用配置页「清空历史数据」（带二次确认）清理。

---

## v1.0.1（2026-08-20）

### 概述

本版本聚焦“历史数据管理”，引入了**自动保留清理**与**二次确认清空**两个能力，解决插件长时间运行后 SQLite 历史数据缓慢膨胀、以及用户误清空不可恢复数据的问题。功能层（流量统计、站点细分、月度阈值限速、月初自动恢复）在 v1.0.0 已完整并经过实测，本版本为在其上的运维向增强。

### 新功能

**1. 历史数据自动保留清理（可配置）**

- 新增配置项「**历史数据保留天数（天）**」，默认 **90**。
- 每次采集（含手动「立即采集」）结束后，自动删除超过保留天数的历史记录，库文件不再无限增长。
- 填 `0` 可完全关闭自动清理（不推荐长期如此，仅兼容需要保留全量历史的场景）。
- 删除的是聚合的历史流量行，不影响当前数据；删除行数会打印到 MP 业务日志。

**2. 手动清空历史数据（带二次确认保护）**

- 配置页新增「**清空历史数据**」按钮。
- 点击后弹出确认对话框，必须手动输入「**清空**」二字，「确认清空」按钮才会启用并真正执行——防止误触导致不可恢复数据丢失。
- 后端新增接口 `POST /api/v1/plugin/DownloaderTraffic/clear`，一次清空 `traffic_records`（流量历史）与 `torrent_snapshots`（种子基准快照）两张表，并返回删除行数。
- 清空后无需额外处理，下一次采集会以各种子**当前累计值为基准重新入账**。

### 实现要点

- 清空与保留清理均在既有 `_db_lock` 线程锁保护下执行，与采集 / API 查询互斥，无并发写库风险。
- 前端新增「清空历史数据」使用 Vue 联邦页面（`dist` 已重新构建），MP 加载时自动获取新配置界面。
- `node_modules` / `package-lock.json` 已通过 `.gitignore` 排除，不进版本库。

### 使用与升级说明

1. 在 MP 插件市场将本插件更新到 **v1.0.1**，并重启 MoviePilot（或重新载入插件）以重新注册 API 与定时任务。
2. 浏览器**强制刷新**（Windows：`Ctrl+Shift+R`）清除旧的前端 dist 缓存，确保加载到新配置界面。
3. 配置页如有需要，调整「历史数据保留天数」；默认 90 天即可满足绝大多数场景。
4. 已验证功能：流量统计、站点细分、月度阈值限速、月初自动恢复、测试限速/恢复按钮，均不受本版本影响。

### 注意事项

- 「清空历史数据」为**不可恢复**的**永久删除**操作，仅在需整套归零、或清除早期脏数据时使用；正常日常维护请优先依赖「历史数据保留天数」自动清理。
- 停止采集（停服 / 断网）期间的流量不会补齐（统计基于种子累计差量），与 v1.0.0 一致。

---

## v1.0.0（2026-08-20）

正式首发版本。按年 / 月 / 日统计 qBittorrent / Transmission 的上传、下载流量，并按 tracker 域名细分到 MP「站点设置」中的每个 PT 站点名；支持月度上传阈值超限后自动对所选下载器设置全局上传 / 下载限速，并按可配置的 Cron（默认每月 1 号 00:30）自动恢复；配置页提供「测试限速 / 测试月初恢复 / 立即采集」按钮。

详细功能与配置项见仓库 [README.md](README.md)。

## 版本兼容性

本插件同时兼容 MoviePilot **V2 与 V3**（采用官方「V3 默认兼容 V2 实现」方式，不额外维护 `plugins.v3/` 副本）。详见 [README.md](README.md)「版本兼容性（V2 / V3）」章节。